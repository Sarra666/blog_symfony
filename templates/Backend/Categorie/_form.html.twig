{{ form_start(form, {'attr': {'class': 'form card w-50 mx-auto p-3'}}) }}
{{ form_widget(form) }}
<div class="text-center">
	<button type="submit" class="btn btn-secondary">{{ button_label|default('save') }}</button>
</div>
{{ form_end(form) }}
